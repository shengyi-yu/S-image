# 项目理解指南（Sheng-image 图片站 + AI 助手）

> 用途：本指南帮你**按正确顺序理解整个项目**，不是用来背的，而是配合代码"跟读"的。
> 读完标准：能不看代码画出架构图，能讲清三条主流程的"入口 → 流向 → 失败处理"。

---

## 0. 项目是什么（一句话版）

**一个带 AI 能力的图片管理网站**：用户上传图片（存腾讯云 COS）、管理私有空间、管理员审核；同时接入了一个 AI 助手，能自动给图片打标签/写描述（入库后可搜索），还能在聊天窗口里对话、搜图、分析图片。

技术栈：**Java Spring Boot 2.7 + MyBatis-Plus + MySQL + Redis + 腾讯云 COS**（业务后端）＋ **Python FastAPI + LangChain/LangGraph + DeepSeek/多模态/Tavily**（AI 服务）＋ **Vue3 + Vite + Ant Design Vue**（前端）。

---

## 1. 整体架构图（先记住这张图）

```
浏览器 Vue 前端 (http://localhost:5173)
   │  HTTP + Cookie(登录态)
   ▼
Java Spring Boot 后端 (http://localhost:8123，context-path=/api)
   │
   ├── user 模块：注册 / 登录 / Redis 会话 / 权限
   ├── picture 模块：上传(COS) / 列表 / 详情 / 搜索 / 审核
   ├── space 模块：私有空间 / 额度管理
   └── ai 模块：AiAgentClient + PictureAiService（HTTP 代理）
   │  HTTP POST
   ▼
Python FastAPI AI 服务 (http://localhost:8000)
   ├── /api/ai/chat    对话（Router Agent 自动选工具）
   ├── /api/ai/analyze 图片分析（结构化 JSON）
   └── services/agents/  Router / Chat / Image / Search 四个智能体

MySQL (S-images 库：user / picture / space 表)
Redis (session 会话 + 列表缓存)
腾讯云 COS (图片文件本体)
```

**三条主流程**（下面第 3 节逐个读）：
1. 登录线 —— 懂"鉴权"
2. 上传线 —— 懂"核心业务 + AI 打标"
3. AI 对话线 —— 懂"跨语言集成"

---

## 2. 目录结构地图（每个目录是干嘛的）

### 2.1 Java 后端（仓库根目录 Sheng-image）

```
src/main/java/cn/com/laning/shengimage/
├── ShengImageApplication.java   # 入口：@SpringBootApplication + @EnableAsync + @MapperScan
├── common/       # 统一响应：BaseResponse{code,message,data} + ResultUtils.success()/error()
├── exception/    # 异常体系：ErrorCode(错误码枚举) + BusinessException(业务异常)
│                 #           + GlobalExceptionHandler(全局兜底) + ThrowUtils.throwIf(简写)
├── config/       # 配置类：CorsConfig(跨域) CosClientConfig(COS) MyBatisPlusConfig JsonConfig
├── controller/   # 接收 HTTP 请求（薄），调 service（厚）
│                 #   UserController / PictureController / SpaceController / FileController
│                 #   AiController(新) / MainController(健康检查)
├── service/      # 业务接口 + 实现（核心逻辑都在这）
│                 #   UserServiceImpl / PictureServiceImpl / SpaceServiceImpl
│                 #   PictureAiService(新)：异步 AI 补全
├── mapper/       # MyBatis-Plus 的 Mapper（实体 ↔ 表）
├── model/        # 数据模型
│   ├── entity/   #   表实体：User / Picture / Space（字段=表列）
│   ├── dto/      #   请求体：UserLoginRequest、PictureUploadRequest、AiChatRequest(新)...
│   ├── vo/       #   返回体：LoginUserVO、PictureVO(含aiTags/aiDescription)...
│   └── enums/    #   枚举：UserRoleEnum(角色) PictureReviewStatusEnum(审核) SpaceLevelEnum
├── manager/      # 通用能力：CosManager(COS上传/压缩) AiAgentClient(新,调Python)
│   └── upload/   #   上传模板：PictureUploadTemplate(模板方法) + File/Url 两个实现
├── annotation/   # @AuthCheck 自定义注解
└── aop/          # AuthInterceptor：AOP 切面做登录/角色校验
```

### 2.2 Python AI 服务（ai-agent/ 目录）

```
ai-agent/
├── main.py               # FastAPI 入口：建 app、开 CORS、挂路由、启动时注册 Nacos(可忽略)
├── config/settings.py    # 配置（读 .env）：DeepSeek key、多模态 key、Tavily key
├── api/routes.py         # 接口：/health  /chat  /analyze(新)
├── services/
│   ├── nacos_client.py   # Nacos 注册（当前没用上，直连即可）
│   └── agents/
│       ├── router.py         # RouterAgent：主智能体，create_agent + 3 个工具
│       ├── chat_agent.py     # ChatAgent：普通对话（DeepSeek）
│       ├── image_agent.py    # ImageRecognitionAgent：多模态图片分析
│       └── search_agent.py   # SearchAgent：Tavily 搜索（图片/网页）
└── static/index.html     # 独立的聊天测试页（老页面，前端已收口到 Vue）
```

### 2.3 Vue 前端（E:\vue-code\Sheng-image）

```
src/
├── request.ts            # axios 封装：baseURL=8123、withCredentials、40100 跳登录
├── api/                  # 接口函数：userController.ts / pictureController.ts ...
│                         #   （openapi 自动生成 + aiController.ts 手写的新接口）
├── pages/                # 页面：HomePage(图片库) PictureDetailPage(详情)
│                         #   ChatPage(新,AI对话) user/ 登录注册 admin/ 管理页
├── components/           # 组件：PictureList(卡片列表) PictureUpload 等
├── router/index.ts       # 路由表
└── stores/               # 登录态 useLoginUserStore / 主题 useThemeStore
```

---

## 3. 三条主流程代码导读（核心！按顺序读，别跳）

> 每条线都回答三个问题：**入口在哪？数据怎么流？失败了怎么办？**

### 流程 A：登录（读懂 = 懂鉴权）

| 顺序 | 文件 | 看什么 |
|---|---|---|
| 1 | Vue `pages/user/UserLoginPage.vue` | 点登录调 `loginUsingPost` |
| 2 | Vue `api/userController.ts` | POST `/api/user/login` |
| 3 | Java `controller/UserController.java` | `userLogin()` 收请求 |
| 4 | Java `service/impl/UserServiceImpl.java` | 查库(userAccount + MD5密码) → **写 session**（spring-session 存 Redis）→ 返回 LoginUserVO |
| 5 | Java `model/entity/User.java` | 表结构对应 |
| 6 | Java `aop/AuthInterceptor.java` | `@AuthCheck` 切面：每个请求先 `getLoginUser`（从 session 取），没登录抛 40100 |

**失败处理**：未登录/密码错 → `BusinessException` → `GlobalExceptionHandler` 统一转成 `{code,message,data}` → 前端 `request.ts` 拦截器看到 40100 跳登录页。

**要点**：登录态 = Redis 里的 session（`spring-session-data-redis`），前端靠 Cookie（`withCredentials`）自动带上。

### 流程 B：图片上传 + AI 自动打标（读懂 = 懂核心业务）

| 顺序 | 文件 | 看什么 |
|---|---|---|
| 1 | Vue `components/UrlPictureUpload.vue` / `AddPicturePage.vue` | 填图片 URL → 调上传接口 |
| 2 | Java `controller/PictureController.java` | `uploadPictureByUrl()`（POST /picture/upload） |
| 3 | Java `service/impl/PictureServiceImpl.java` | `uploadPicture()`：校验空间额度 → 上传 → 事务入库 → 异步 AI |
| 4 | Java `manager/upload/PictureUploadTemplate.java` | **模板方法模式**：校验→下载→COS上传→取图信息，两个实现类 File/Url |
| 5 | Java `manager/CosManager.java` | 调 COS：原图 + 自动转 webp + 生成缩略图 |
| 6 | Java `service/PictureAiService.java`(新) | `@Async fillAiInfo`：调 AI 分析 → 写回 ai_tags/ai_description |
| 7 | Java `manager/AiAgentClient.java`(新) | HTTP POST Python `/api/ai/analyze` |
| 8 | Python `api/routes.py` 的 `/analyze`(新) | 调多模态模型 → 返回结构化 JSON |
| 9 | Java `service/impl/PictureServiceImpl.java` 的 `getQueryWrapper` | 搜索词同时匹配 name/introduction/**ai_tags/ai_description** |

**失败处理**：AI 分析失败只 `log.warn`，**不影响上传成功**（异步 + try/catch 兜底）。

**要点**：
- 事务里做了两件事：图片入库 + 更新空间额度（`totalSize/totalCount`）
- `@Async` 必须放独立 Bean（`PictureAiService`），同类内 `this` 调用异步会静默失效
- AI 只更新两个字段（`updateById` 忽略 null，不会覆盖用户手填内容）
- 审核状态：管理员上传自动过审，普通用户待审核（`fileReviewParams`）

### 流程 C：AI 对话（读懂 = 懂跨语言集成）

| 顺序 | 文件 | 看什么 |
|---|---|---|
| 1 | Vue `pages/ChatPage.vue`(新) | 输入消息 → `chatUsingPost`（threadId=登录用户id） |
| 2 | Vue `api/aiController.ts`(新) | POST `/api/ai/chat`（带 Cookie） |
| 3 | Java `controller/AiController.java`(新) | `@AuthCheck` 强制登录 → 调 AiAgentClient |
| 4 | Java `manager/AiAgentClient.java`(新) | `chat()`：HTTP POST Python 8000 `/api/ai/chat` |
| 5 | Python `api/routes.py` 的 `/chat` | 调 RouterAgent |
| 6 | Python `services/agents/router.py` | `create_agent`(LangGraph) + 3 个工具：chat / search_image / analyze_image |
| 7 | Python `services/agents/*.py` | 三个工具的实现（DeepSeek / Tavily / mimo多模态） |

**失败处理**：Java 端 Python 不可用时返回 `SYSTEM_ERROR("AI 服务暂时不可用")`，前端在气泡里展示错误信息。

**要点**：
- 前端只认识 8123，AI 是"内部服务"，收口到 Java 做鉴权/限流
- 每个用户对话隔离：`threadId = 登录用户 id`（Python 端 InMemorySaver 按 thread_id 存记忆）
- 为什么跨语言？LangChain/多模态生态在 Python 最成熟；Java 专注业务

---

## 4. FastAPI 最小读法（你不需要会写，只需要会读）

FastAPI 比 Spring Boot 简单一个量级，读它只要三句话：

```python
@router.post("/chat")                    # ① 装饰器 = 定义一个 POST 接口，路径是 /api/ai/chat
async def chat(request: ChatRequest):    # ② 函数 = 处理逻辑；参数类型就是请求体结构
    response = await router.route(...)   #    await = 异步等待（类似 Java 的 CompletableFuture）
    return ChatResponse(code=0, data=response)  # ③ 返回什么，Java 就收到什么
```

- `BaseModel` 类（ChatRequest/ChatResponse）= 请求/响应的结构定义（类似 Java 的 DTO）
- 读的顺序：`main.py`（入口）→ `api/routes.py`（有哪些接口）→ `services/agents/`（接口背后干了啥）
- 不用学的：FastAPI 的高级特性、依赖注入、WebSocket —— 这个项目用不到

---

## 5. 关键知识点速查（面试随时能接住）

| 概念 | 项目里的体现 | 一句话解释 |
|---|---|---|
| 三层架构 | Controller→Service→Mapper | 接请求 / 写逻辑 / 操作数据库 |
| MyBatis-Plus | `ServiceImpl` + `QueryWrapper` | 实体=表，方法=SQL，不用手写 SQL |
| 逻辑删除 | `@TableLogic isDelete` | 删数据不是真删，标记 isDelete=1 |
| Redis 会话 | spring-session | 登录态存 Redis，重启不丢、可集群 |
| AOP 鉴权 | @AuthCheck + AuthInterceptor | 注解声明"要登录/要管理员"，切面统一校验 |
| 模板方法 | PictureUploadTemplate | 上传流程骨架固定，文件/URL 两种来源只换"怎么拿图" |
| 异步 | @Async + 独立 Bean | AI 打标不阻塞上传响应 |
| 降级 | catch + log + 返回 null | AI 挂了业务照常 |
| 部分更新 | updateById 忽略 null | 只更新 AI 字段，不覆盖用户内容 |
| 跨语言 | HTTP 直连 + 统一 {code,data} 契约 | 两个服务各自封装，调用方只认 code |
| 多级缓存 | Caffeine 本地 + Redis | 列表接口先查本地缓存，再查 Redis，最后查库 |

---

## 6. 环境与构建的"坑"（当时修过的，别踩第二次）

- **JDK 24 + Lombok**：Spring Boot 2.7.6 默认带的 Lombok 1.18.24 不兼容 JDK24（注解不生效，全是"找不到符号"）→ pom 已升到 1.18.42
- **JDK 23+ 的 javac**：不再默认运行 classpath 上的注解处理器 → pom 的 maven-compiler-plugin 已加 `<arg>-proc:full</arg>`
- **命令行构建**：本机 Maven 的本地仓库在沙箱外，用 `mvn -s .mvn-settings.xml`（仓库指向工作区内 `.m2-repo`）；**IDEA 里开发不需要这些**
- **COS 域名**：自定义域名 `sheng.laning.com.cn` 当前不可访问，`application-local.yaml` 已改用 COS 默认域名 `yuan-1378104012.cos.ap-beijing.myqcloud.com`
- **profile**：`application.yaml` 已启用 `spring.profiles.active: local`，密钥在 gitignored 的 `application-local.yaml`

---

## 7. 本地启动（三个服务）

```bash
# 1. Python AI 服务（ai-agent 目录）
cd ai-agent && venv313\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
# 或 python main.py（开发热重载）

# 2. Java 后端（仓库根目录）
mvn -s .mvn-settings.xml spring-boot:run

# 3. Vue 前端（E:\vue-code\Sheng-image）
npm run dev   # → http://localhost:5173
```

测试账号：`aitest2026` / `12345678`（普通用户）；管理员账号用课程里的（可去 user 表看 role='admin' 的用户）。

---

## 8. 面试 Q&A（预测题 + 参考答法）

**Q1：项目介绍（1 分钟）**
> "一个图片管理网站，我给它接入了 AI：用户上传图片后，系统自动用多模态模型生成标签和描述写进数据库，之后能靠 AI 标签搜索到图；聊天助手能对话、搜图、分析图片。后端 Java 管业务（用户/空间/审核/上传 COS），Python 管 AI 编排（LangChain），我负责从需求拆分、两个服务联调到 AI 入库的完整闭环。"

**Q2：为什么 Java 和 Python 两个服务？**
> "LLM 生态（LangChain、多模态）在 Python 最成熟，Java 侧重业务稳定性。两个服务各干各的，通过 HTTP 通信，职责清晰。"

**Q3：为什么先 HTTP 直连，不上 Nacos/Feign？**
> "两个服务都在本机，直连最简。我把 AI 地址配在配置文件里，随时能演进；上注册中心的收益（动态发现、多实例）在这个规模还不明显，等部署多实例再加，成本更低。"

**Q4：AI 挂了会影响上传吗？**
> "不会。AI 打标是异步的（@Async），且全链路 catch 异常只记日志；上传成功是主流程，AI 是锦上添花。"

**Q5：@Async 为什么放独立类？**
> "Spring 的异步靠代理生效，同类内 this 调用不走代理会静默失效，所以放在 PictureAiService 这个独立 Bean 里由 PictureServiceImpl 调用。"

**Q6：ai_tags 为什么用 @TableField？**
> "库里大部分列是驼峰（createTime），但 ai_tags 是下划线风格，MyBatis-Plus 默认用字段名当列名，不显式指定会报列不存在。"

**Q7：登录鉴权怎么做的？**
> "登录成功把用户存进 Redis Session，前端带 Cookie；需要登录的接口加 @AuthCheck，AOP 切面统一从 Session 取用户，取不到返回 40100。"

**Q8：AI 标签怎么参与搜索？**
> "列表查询的 QueryWrapper 里 searchText 同时 like name、introduction、ai_tags、ai_description，所以 AI 生成的标签也能搜到。"

**Q9：不同用户对话会串吗？**
> "Python 端按 thread_id 存对话记忆，我让前端把登录用户 id 作为 thread_id 传过去，每个用户一段独立对话。"

**Q10：FastAPI 你熟吗？**
> "基础框架我会读会改：路由装饰器定义接口、Pydantic 定义结构、agents 目录里是具体实现。这个项目里 FastAPI 只做了一层很薄的 HTTP 封装，核心逻辑在 LangChain 的智能体里。"

---

## 9. 自测清单（能全勾上 = 真的懂了）

- [ ] 不看代码能画出第 1 节的架构图
- [ ] 能讲清登录请求从 Vue 到 Redis 的完整路径
- [ ] 能讲清上传后 AI 标签是怎么进数据库的（含异步、降级）
- [ ] 能说出 `AiAgentClient` 的两个方法分别调 Python 的哪个接口
- [ ] 能说出 `RouterAgent` 有哪些工具、分别在什么场景被调用
- [ ] 能解释 @AuthCheck、@Async、@TableField、模板方法四个点在项目里的作用
- [ ] 能回答第 8 节至少 6 个问题
- [ ] 能独立把三个服务启动起来并演示一遍

---

*本指南对应代码状态：AI 打标入库 + AI 标签搜索 + 聊天收口到 Java + Vue 聊天页（已完成并验证）。*
